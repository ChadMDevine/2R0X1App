<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.a2r0x1app">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_action_f35"
            android:label="@string/app_name"
            android:roundIcon="@drawable/ic_action_f35"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MetricActivity"/>
        <activity android:name=".McRateActivity"/>
        <activity android:name=".AfiActivity"/>
        <activity android:name=".NoteActivity"
                android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".HistoryNotesActivity" />
    </application>

</manifest>